{
  "name": "timetables",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:vite": "vite build --watch",
    "dev": "wrangler pages dev dist --compatibility-date=2026-01-01",
    "build": "vite build && node scripts/embed-admin-page.mjs",
    "deploy": "vite build && wrangler pages deploy dist",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p functions/tsconfig.json && tsc --noEmit -p tests/tsconfig.frontend.json && tsc --noEmit -p tests/tsconfig.functions.json",
    "db:migrate:local": "wrangler d1 migrations apply timetables-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply timetables-db --remote",
    "db:migrate:preview": "wrangler d1 migrations apply timetables-db-preview --remote --config wrangler.preview-db.toml",
    "sync-courses": "sh scripts/sync-courses.sh",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.9",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  }
}
